package com.example.arthas.notifications

enum class NotificationRate {

    HIGH, // makes a sound and appears as a heads-up notiﬁcation
    DEFAULT, // makes a sound
    LOW, // no sound
    MIN // only status bar
}